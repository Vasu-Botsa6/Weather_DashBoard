import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Weather Dashboard</h1>
      {/* Add any other branding elements */}
    </header>
  );
};

export default Header;
